import 'package:flutter/material.dart';

/// --- [de-info]

// name
const String de_NAME = "jappe-os desktop";

// version
const String de_VERSION = "alpha: 1.1.3";

/// --- [themes]

// theme
// const dsktp_THEME = Color(0xFF212121); /// Default available color themes: light: 0xFFf5f6fa; dark: 0xFF222f3e; // The dafault color is light

// blur color
final Color dsktp_BLUR_COLOR_DARK = Colors.black.withOpacity(0.5);
final Color dsktp_BLUR_COLOR_LIGHT = Colors.white.withOpacity(0.5);

// blur color window
final Color dsktp_BLUR_COLOR_DARK_WINDOW = Color.fromARGB(167, 27, 27, 27);
final Color dsktp_BLUR_COLOR_LIGHT_WINDOW = Color.fromARGB(167, 206, 206, 206);

// text color
const dsktp_TEXT_COLOR_LIGHT = Color(0xFFFFFFFF);
const dsktp_TEXT_COLOR_DARK = Color(0xFF000000);

// accent color
const dsktp_ACCENT_COLOR = Colors.blueAccent;
const dsktp_SECONDARY_ACCENT_COLOR = Color(0xFFCFCFCF);

// desktop wallpaper
const String dsktpWallpaper =
    "lib/images/desktop/backgrounds/wallpaper3.jpg"; // This should search the picture from a local folder in the future.

// ----------------------------------------------------------------------------------